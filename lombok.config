# lombokの設定ファイル
#   http://projectlombok.org/features/configuration.html

# lombok がこれより上の階層 lombok.configを探しに行かないようにする
config.stopBubbling = true

# @Slf4j で生成されるロガーの変数名を指定する。
lombok.log.fieldName = logger
